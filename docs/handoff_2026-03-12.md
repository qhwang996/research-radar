# Session Handoff (2026-03-12)

## Latest Code Changes

- `src/crawlers/ccs_crawler.py`
  - Added filtering for non-full-paper CCS entries in both official-page parsing and DBLP fallback parsing.
  - Skips titles starting with `Workshop`, `The Nth Workshop`, `Poster:`, `Demo:`, `Tutorial:`, `Keynote:`, and `Panel:`.
  - Keeps `SoK:` entries.
  - Logs skipped-count per year.
- `prompts/relevance_score.md`
- `src/pipelines/llm_relevance.py`
  - Relevance prompt updated to the newer web-security-centric rubric.
  - Prompt file and embedded `DEFAULT_PROMPT_TEMPLATE` are intentionally identical.
  - `relevance_version` default changed from `v1` to `v2`, so relevance cache keys are now `relevance_v2_{canonical_id}`.
- `src/pipelines/enrichment.py`
- `src/pipelines/llm_relevance.py`
  - Both pipelines now support parallel processing with `ThreadPoolExecutor`.
  - Default `max_workers=8`.
  - Worker threads create their own DB sessions and save independently.
  - Single-item failures are isolated and do not stop the batch.
- `src/cli/process.py`
  - Added `--workers` to `enrich`, `llm-relevance`, and `run`.
  - Default is `8`.
- `src/cli/crawl.py`
  - CLI source slug handling tolerates both underscore and hyphen variants, so `usenix_security` and `usenix-security` both resolve.

## Latest Test Status

- Full validation completed with:
  - `conda run -n research-radar python -m pytest -v`
- Result:
  - `101 passed, 5 subtests passed`

## Operational Notes

- If future work uses Anthropic for `llm-relevance`, the current third-party endpoint may reject the default STANDARD tier model.
  - Known-good workaround:
    - `ANTHROPIC_MODEL_STANDARD=claude-haiku-4-5-20251001`
- LLM cache files are hash-named on disk, not plaintext by cache key.
  - The logical cache key is still `relevance_v2_{canonical_id}` or `enrichment_v1_{canonical_id}`.
  - The actual file name is `sha256(cache_key).json`.

## Last Known Corpus State

- Artifact total: `4116`
- Source distribution:
  - `ACM CCS=1392`
  - `USENIX Security=1556`
  - `NDSS=794`
  - `IEEE S&P=374`
- Coverage:
  - `summary_l1` missing: `0`
  - `tags` missing: `0`
  - `llm_relevance_score` missing: `0`
- Abstract coverage:
  - with abstract: `794`
  - without abstract: `3322`

## Recommended Starting Point For The Next Session

- Read this file first.
- If the next task is data/LLM pipeline work, also read:
  - `docs/live_run_report_2026-03-11.md`
  - `docs/design/06_llm_strategy.md`
  - `docs/design/07_idempotency.md`
  - `docs/specs/p7_2_llm_relevance_scoring.md`
