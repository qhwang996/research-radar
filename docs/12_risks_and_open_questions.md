# Risks and Open Questions

本文档记录项目的风险和待解决的问题。

---

## 技术风险

### R1: LLM成本超预算

**风险描述**：
- 预算：$30-60/月
- 如果每天处理50篇论文，每篇生成3层摘要，成本可能超标

**缓解措施**：
1. 严格的缓存策略（相同内容不重复调用）
2. 分级处理（L1用便宜模型，L3仅按需生成）
3. 批处理优化（一次prompt处理多篇）
4. 监控token消耗，超过阈值告警
5. Phase 1只生成L1摘要

**当前状态**：待验证（需要试运行后确认实际成本）

---

### R2: 爬虫失效

**风险描述**：
- 会议网站改版导致爬虫失效
- 网站反爬虫机制
- 网络不稳定

**缓解措施**：
1. 实现重试机制（3次重试，指数退避）
2. 保存原始HTML，便于调试
3. 添加User-Agent和请求延迟
4. 失败时发送告警
5. 支持手动导入数据

**当前状态**：部分实现（现有爬虫有基础错误处理）

---

### R2.1: CCS数据源结构不稳定

**风险描述**：
- ACM CCS 2025 官方 accepted papers 页面首屏 HTML 中列表为空，内容依赖前端运行时加载
- ACM Digital Library proceedings 页面存在反爬虫检查，稳定性不足
- 直接依赖单一页面结构会导致爬虫在无浏览器环境下失效

**已观察到的问题（2026-03-09）**：
- `https://www.sigsac.org/ccs/CCS2025/accepted-papers/` 返回的静态 HTML 中 `First Cycle` 和 `Second Cycle` 的 `<ul>` 为空
- 用户提供的 ACM DL proceedings 链接存在反爬虫风险，不适合作为唯一抓取路径

**缓解措施**：
1. 官方页面作为第一抓取源，优先尝试静态解析
2. 当官方页面没有静态论文列表时，回退到更稳定的结构化来源
3. 当前实现已加入 DBLP fallback，保证 P1.1 阶段可以稳定获取 CCS 标题、作者和外链
4. 后续如需更完整元数据，再评估浏览器渲染或官方 API 方案

**当前状态**：已识别，已通过 fallback 缓解

---

### R2.2: 博客站点模板频繁漂移

**风险描述**：
- 博客首页常在不改 URL 的前提下更换卡片结构、标题层级和作者字段
- 静态 parser 容易只对历史模板有效，真实线上页面可能返回 0 条结果

**已观察到的问题（2026-03-09）**：
- Google Project Zero 当前首页已不再稳定匹配旧 Blogger `post-outer` 结构
- Cloudflare Security 标签页当前使用新的文章卡片链接模式，不再依赖 `/the-latest/`

**缓解措施**：
1. parser 尽量基于语义字段而不是单一 class 名
2. 先做本地 parser 测试，再做线上 live fetch 验证
3. 当前实现已扩展为兼容 Project Zero 和 Cloudflare 的现行页面结构

**当前状态**：已识别，已通过 live validation 修复

---

### R2.3: Raw JSON 历史格式不一致

**风险描述**：
- 早期爬虫输出与当前统一爬虫输出字段不一致
- normalization 若只支持单一格式，会导致历史 raw 数据无法重放
- 同一来源在不同阶段的 key 命名不同，容易造成 replay 失败或字段缺失

**已观察到的问题（2026-03-10）**：
- 历史 raw 文件使用 `papers` + `url` 结构
- 当前统一爬虫输出使用 `items` + `source_type` 结构
- 博客与论文的内容 URL 字段也不一致（`paper_url` / `article_url` / `source_url`）

**缓解措施**：
1. NormalizationPipeline 同时兼容 legacy 和 current 两种 payload 结构
2. 优先提取内容级 URL，再回退到 listing URL，避免 artifact 丢失可点击链接
3. canonical_id 基于稳定 title key 生成，避免重放时重复入库

**当前状态**：已识别，已在 P2.1 中兼容

---

### R2.4: Raw 文件重处理暂不处理删除场景

**风险描述**：
- 当前 P2.1 已支持 `content_hash` 变化后重新处理 raw 文件
- 但如果新版 raw 文件删除了旧条目，pipeline 目前只会 upsert 当前仍存在的内容
- 这意味着历史上由该文件导入、但本次文件中已不存在的 artifact 不会被自动归档或删除

**已观察到的问题（2026-03-10）**：
- RawFetch 记录的是文件级处理状态，不维护 artifact 与 raw 文件条目的完整映射
- 因此增量更新已经覆盖“新增/变更”，但还没有覆盖“删除”

**缓解措施**：
1. Phase 1 保持 append/update 语义，不在 normalization 阶段做 destructive delete
2. 后续可为 RawFetch 增加输出 artifact 映射表，支持 diff 后归档失效条目
3. 在报告和评分阶段默认只消费 active artifact，避免误用未来可能归档的数据

**当前状态**：已识别，暂未处理

---

### R3: 数据质量问题

**风险描述**：
- 爬取的数据不完整（缺少作者、摘要）
- LLM生成的摘要质量差
- 评分不准确

**缓解措施**：
1. 数据验证（标记data_completeness）
2. LLM prompt优化
3. 人工review前几周的输出
4. 根据反馈调整评分权重
5. 保留原始数据，支持重新处理

**当前状态**：待实现

---

### R4: 系统稳定性

**风险描述**：
- 长期运行可能崩溃
- 数据库损坏
- 磁盘空间不足

**缓解措施**：
1. 充分的异常处理
2. 定期数据库备份
3. 磁盘空间监控
4. 日志轮转
5. 健康检查机制

**当前状态**：待实现

---

### R4.1: 开发环境依赖未对齐

**风险描述**：
- 项目专用环境可能缺少运行时和测试依赖
- 代码已实现但无法及时做运行态验证
- 不同机器或不同shell下的环境状态可能不一致

**已观察到的问题（2026-03-09）**：
- `research-radar` conda 环境中缺少 `sqlalchemy`
- `research-radar` conda 环境中缺少 `pytest`
- 导致 P0.1 数据模型只能完成语法检查，无法执行数据库 CRUD 测试

**缓解措施**：
1. 明确维护项目环境依赖清单（requirements.txt 或 conda env 文件）
2. 每次开始新优先级实现前先验证关键依赖是否可导入
3. 优先在项目专用环境中运行测试，不依赖系统 Python
4. 后续补充一条环境初始化命令或 Makefile/脚本，降低环境漂移

**处理结果（2026-03-09）**：
- 已在 `research-radar` conda 环境中安装 `sqlalchemy` 和 `pytest`
- 执行 `conda run -n research-radar python -m pytest tests -q`
- 测试结果：`6 passed`

**当前状态**：已解决

---

## 产品风险

### R5: 输出质量不符合预期

**风险描述**：
- 每周推荐的3个方向不够好
- 推荐的论文不相关
- 错过重要论文

**缓解措施**：
1. 试运行期间持续调整参数
2. 收集用户反馈
3. 多策略并行（Phase 3）
4. 人工review机制
5. 支持手动添加论文

**当前状态**：待验证

---

### R6: 用户时间投入过多

**风险描述**：
- 设计目标是每天5-10分钟，但实际可能需要更多时间
- 报告太长，难以快速浏览

**缓解措施**：
1. 优化报告格式（突出重点）
2. 实现多层次摘要（L1快速扫描）
3. 减少推荐数量（宁缺毋滥）
4. 简化反馈流程

**当前状态**：待验证

---

## 开放问题

### Q1: 主题提取策略

**问题**：
- Phase 1是否需要复杂的主题提取？
- 简单的关键词聚类是否足够？
- 还是直接用LLM提取主题？

**当前倾向**：
- Phase 1跳过主题提取，直接推荐Top 10论文
- Phase 2再实现主题聚类

**待决策**：试运行后根据实际需求决定

---

### Q2: 候选方向生成逻辑

**问题**：
- 如何从论文生成"研究方向"？
- 需要多少篇论文才能形成一个方向？
- 如何构建证据链？

**当前倾向**：
- Phase 1简化为"Top 3主题"（基于关键词聚类）
- Phase 2实现完整的方向生成逻辑（LLM驱动）

**待决策**：试运行后根据实际需求决定

---

### Q3: Profile更新策略

**问题**：
- 如何从反馈学习用户偏好？
- 简单的关键词权重调整？
- 还是复杂的embedding模型？

**当前倾向**：
- Phase 1手动更新Profile配置文件
- Phase 2实现简单的关键词权重调整
- Phase 3考虑更复杂的模型

**待决策**：试运行后根据反馈效果决定

---

### Q4: 数据源优先级

**问题**：
- Phase 1只做NDSS和S&P够吗？
- 什么时候添加CCS和USENIX？
- arXiv是否必要？

**当前倾向**：
- Phase 1: NDSS + S&P（已有爬虫）
- Phase 2: 添加CCS + USENIX
- arXiv可选（数据量大，质量参差）

**待决策**：试运行后根据论文数量决定

---

### Q5: 演化追踪的价值

**问题**：
- 追踪引用数、讨论数是否有价值？
- 多久更新一次？
- 如何获取这些数据？

**当前倾向**：
- Phase 2实现基础版本
- 每周更新一次
- 使用Google Scholar或Semantic Scholar API

**待决策**：Phase 2开始前评估可行性

---

### Q6: 报告格式

**问题**：
- Markdown格式是否足够？
- 是否需要HTML版本？
- 是否需要可视化（图表）？

**当前倾向**：
- Phase 1: 纯Markdown
- Phase 2: 考虑添加简单图表（趋势曲线）
- 不做Web UI

**待决策**：试运行后根据用户体验决定

---

### Q7: 多年数据的处理策略

**问题**：
- 一次性爬取5年数据（2022-2026）会不会太多？
- 如何避免"旧论文"淹没"新论文"？
- 历史数据的价值是什么？

**当前倾向**：
- 一次性爬取5年数据（建立基线）
- 评分时给新论文更高权重（时效性评分）
- 历史数据用于趋势分析（Phase 2）

**待决策**：试运行后根据实际效果调整

---

### Q8: 反馈粒度

**问题**：
- 对每篇论文都反馈太累？
- 只对候选方向反馈够吗？
- 轻反馈（like/dislike）vs 深反馈（pros/cons）？

**当前倾向**：
- Phase 1: 只对Top 10论文提供轻反馈（interested/not_interested）
- Phase 2: 对候选方向提供深反馈（pros/cons/unknown）

**待决策**：试运行后根据用户时间投入决定

---

## 待验证假设

### H1: 用户能坚持每天5-10分钟

**假设**：用户能坚持每天花5-10分钟浏览报告并提供反馈

**验证方法**：试运行2周，记录实际时间投入

**如果假设不成立**：
- 减少推荐数量
- 优化报告格式
- 降低反馈频率（改为每周）

---

### H2: LLM生成的摘要有价值

**假设**：LLM生成的一句话摘要能帮助快速判断论文价值

**验证方法**：试运行期间对比原始标题和摘要的效果

**如果假设不成立**：
- 优化prompt
- 或者只用标题+关键词

---

### H3: 评分策略有效

**假设**：基于时效性+权威性的评分能有效排序论文

**验证方法**：试运行期间检查Top 10是否符合预期

**如果假设不成立**：
- 调整权重
- 添加相关性评分
- 实现多策略实验

---

### H4: 反馈能改善推荐

**假设**：用户反馈能让系统推荐越来越准确

**验证方法**：对比第1周和第4周的推荐质量

**如果假设不成立**：
- 优化Profile更新逻辑
- 或者改为手动调整偏好

---

## 决策记录

### D1: Phase 1跳过主题提取（2026-03-09）

**决策**：Phase 1不实现复杂的主题提取，直接推荐Top 10论文

**理由**：
- 快速验证核心价值
- 避免过度工程
- 主题提取可以Phase 2再做

**影响**：Phase 1的"候选方向"简化为"Top 3关键词聚类"

---

### D2: Phase 1只支持NDSS和S&P（2026-03-09）

**决策**：Phase 1只爬取NDSS和S&P

**理由**：
- 已有爬虫代码
- 快速启动
- 验证系统价值后再扩展

**影响**：论文数量可能较少（每年约200篇），但足够验证系统

---

### D3: 使用SQLite而非PostgreSQL（2026-03-09）

**决策**：MVP使用SQLite

**理由**：
- 单用户系统
- 部署简单
- 性能足够
- 后续可迁移

**影响**：无需安装数据库服务，降低部署复杂度

---

## 下一步行动

1. **立即开始Phase 1实现**
2. **1-2周内完成MVP**
3. **立即试运行2周**
4. **根据试运行结果更新本文档**
5. **决策Phase 2优先级**
